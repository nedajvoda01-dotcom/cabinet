// Kernel Primitives
// Pure functions for deterministic operations (stable sort, hashing, ID generation)
// No I/O, no side effects - must be reproducible

pub mod stable_sort;
pub mod hash;
pub mod ids;
