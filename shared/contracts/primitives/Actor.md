# Actor

Kind: **object**

Represents the caller identity.

## Fields
- `actorId` (ActorId, required) — Actor identifier bound to the session or integration.
- `actorType` (ActorType, required) — Origin of the actor.
