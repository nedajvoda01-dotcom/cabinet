{
  "name": "cabinet-monorepo",
  "version": "1.0.0",
  "description": "Cabinet Platform Monorepo",
  "private": true,
  "workspaces": [
    "ui/desktop",
    "shared/contracts"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=ui/desktop",
    "build": "npm run build --workspace=ui/desktop",
    "test": "npm run test --workspace=ui/desktop"
  }
}
